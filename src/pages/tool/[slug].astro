---
import '../../styles/global.css';
import { getCollection, getEntry } from 'astro:content';
import SEO from '../../components/SEO.astro';
import Layout from '../../components/Layout.astro';
import slugify from '../../utils/slugify.js';

export async function getStaticPaths() {
  const tools = await getCollection('tools');
  return tools.map(tool => ({
    params: { slug: tool.id.replace(/\.md$/, '') }
  }));
}

const { slug } = Astro.params;
// Remove .md extension if present
const toolId = slug.endsWith('.md') ? slug.slice(0, -3) : slug;
const tool = await getEntry('tools', toolId);

// Build a name→slug lookup for alternatives linking
const allTools = await getCollection('tools');
const toolSlugMap: Record<string, string> = {};
for (const t of allTools) {
  const tSlug = t.id.replace(/\.md$/, '');
  const tName = (t.data.title || t.data.name || '').toLowerCase();
  toolSlugMap[tName] = tSlug;
}

if (!tool) {
  throw new Error(`Tool "${toolId}" not found`);
}

const { data } = tool;

// Related tools: same category, exclude current, top 3 by rank
const relatedTools = allTools
  .filter(t => {
    const tSlug = t.id.replace(/\.md$/, '');
    return t.data.category === data.category && tSlug !== toolId;
  })
  .sort((a, b) => (a.data.rank || 999) - (b.data.rank || 999))
  .slice(0, 3);

const categoryToAppCategory: Record<string, string> = {
  writing: 'Writing & Content Creation',
  image: 'Image Generation & Editing',
  coding: 'Developer Tools & Code Assistance',
  video: 'Video Generation & Editing',
  audio: 'Audio & Voice Generation',
  productivity: 'Productivity & Workflow Automation',
  research: 'Research & Knowledge Management',
  design: 'Design & UI/UX Tools',
  'ai-marketing': 'Marketing & SEO',
};
const appCategory = categoryToAppCategory[data.category || ''] || 'Productivity';

const categoryLabel: Record<string, string> = {
  writing: 'Writing',
  image: 'Image',
  coding: 'Coding',
  video: 'Video',
  audio: 'Audio',
  productivity: 'Productivity',
  research: 'Research',
  design: 'Design',
  'ai-marketing': 'AI Marketing',
};
const catLabel = categoryLabel[data.category || ''] || (data.category || 'Tools');
const catSlug = data.category || 'writing';

const breadcrumbSchema = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://www.toolaroo.net/" },
    { "@type": "ListItem", "position": 2, "name": catLabel, "item": `https://www.toolaroo.net/category/${catSlug}/` },
    { "@type": "ListItem", "position": 3, "name": data.title, "item": `https://www.toolaroo.net/tool/${toolId}/` },
  ]
};

const schemaData = {
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  "name": data.title,
  "description": data.description,
  "url": data.url,
  "applicationCategory": appCategory,
  "operatingSystem": "Web",
};

const seoProps = {
  title: `${data.title} - AI Tool | Toolaroo`,
  description: data.description,
  type: "article",
  schema: [schemaData, breadcrumbSchema]
};
---

<Layout title={data.title} description={data.description} seoProps={seoProps}>
  <!-- Breadcrumb -->
  <nav class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 pt-6 pb-0" aria-label="Breadcrumb">
    <ol class="flex items-center gap-2 text-sm text-slate-500">
      <li><a href="/" class="hover:text-blue-600 transition-colors">Home</a></li>
      <li class="text-slate-300">/</li>
      <li><a href={`/category/${catSlug}/`} class="hover:text-blue-600 transition-colors capitalize">{catLabel}</a></li>
      <li class="text-slate-300">/</li>
      <li class="text-slate-900 font-medium truncate">{data.title}</li>
    </ol>
  </nav>

  <section class="py-12 sm:py-20 pb-3 border-b border-slate-200">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <!-- Tool Logo and Title in Row -->
      <div class="flex items-center justify-center gap-4 mb-6">
        <img
          src={`https://www.google.com/s2/favicons?sz=64&domain=${data.url}`}
          alt={`${data.title} logo`}
          class="w-16 h-16 rounded-lg shadow-md flex-shrink-0"
        />
        <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold text-slate-900 leading-tight">
          {data.title} <span class="bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">AI Tool</span>
        </h1>
      </div>
      <div class="text-center">
      <p class="text-lg sm:text-xl text-slate-600 mb-6">{data.description}</p>
      <div class="flex flex-wrap gap-3 justify-center">
        <span class="px-4 py-2 bg-blue-100 text-blue-700 rounded-lg font-medium capitalize">{data.category}</span>
        {data.pricingModel && (
          <span class={`px-4 py-2 rounded-lg font-medium ${
            data.pricingModel === 'Free' ? 'bg-green-100 text-green-700' :
            data.pricingModel === 'Freemium' ? 'bg-sky-100 text-sky-700' :
            'bg-purple-100 text-purple-700'
          }`}>{data.pricingModel}</span>
        )}
        {data.rating && (
          <span class="px-4 py-2 bg-amber-50 text-amber-700 rounded-lg font-medium">★ {data.rating}/5</span>
        )}
        {data.featured && (
          <span class="px-4 py-2 bg-yellow-100 text-yellow-700 rounded-lg font-medium">⭐ Featured</span>
        )}
      </div>
    </div>
  </section>

  <section class="py-8 sm:py-12">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 space-y-16">
      <!-- Stats -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-12">
        <div class="bg-white p-8 rounded-xl shadow-md border border-slate-200">
          <h3 class="text-sm font-semibold text-slate-600 uppercase mb-2">Rank</h3>
          <p class="text-3xl font-bold text-slate-900">#{data.rank || 'N/A'}</p>
        </div>
        <div class="bg-white p-8 rounded-xl shadow-md border border-slate-200">
          <h3 class="text-sm font-semibold text-slate-600 uppercase mb-2">Status</h3>
          <p class="text-xl font-bold text-slate-900">{data.featured ? '⭐ Featured' : 'Listed'}</p>
        </div>
        <div class="bg-white p-8 rounded-xl shadow-md border border-slate-200">
          <h3 class="text-sm font-semibold text-slate-600 uppercase mb-2">Category</h3>
          <p class="text-xl font-bold text-slate-900 capitalize">{data.category}</p>
        </div>
      </div>

      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row gap-4">
        <a
          href={`/go/${toolId}/`}
          target="_blank"
          rel="noopener noreferrer"
          class="flex-1 text-center px-8 py-4 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors font-semibold shadow-lg hover:shadow-xl"
        >
          Open Tool →
        </a>
        <a
          href={`/category/${slugify(data.category)}/`}
          class="flex-1 text-center px-8 py-4 bg-slate-200 text-slate-900 rounded-lg hover:bg-slate-300 transition-colors font-semibold"
        >
          More {data.category} Tools
        </a>
      </div>

      {/* Who is this for */}
      {data.whoIsItFor && (
        <div class="bg-blue-50 border border-blue-200 rounded-xl p-8">
          <h2 class="text-2xl font-bold text-slate-900 mb-3">Who Is This For?</h2>
          <p class="text-lg text-slate-700 leading-relaxed">{data.whoIsItFor}</p>
        </div>
      )}

      {/* Long Description */}
      {data.longDescription && (
        <div class="prose prose-slate max-w-none">
          <h2 class="text-3xl font-bold text-slate-900 mb-6">About {data.name || data.title}</h2>
          {data.longDescription.split('\n\n').map((para) => (
            <p class="text-lg text-slate-700 leading-relaxed mb-4">{para}</p>
          ))}
        </div>
      )}

      {/* Key Features */}
      {data.features && data.features.length > 0 && (
        <div class="prose prose-slate max-w-none">
          <h2 class="text-3xl font-bold text-slate-900 mb-6">Key Features</h2>
          <ul class="grid grid-cols-1 md:grid-cols-2 gap-4 list-none pl-0">
            {data.features.map((feature) => (
              <li class="flex items-start gap-3 text-lg text-slate-700">
                <span class="text-blue-600 font-bold text-xl mt-1">✨</span>
                <span>{feature}</span>
              </li>
            ))}
          </ul>
        </div>
      )}

      {/* Best Use Cases */}
      {data.useCases && data.useCases.length > 0 && (
        <div class="prose prose-slate max-w-none">
          <h2 class="text-3xl font-bold text-slate-900 mb-6">Best Use Cases</h2>
          <ul class="space-y-2 list-disc list-inside text-lg text-slate-700">
            {data.useCases.map((useCase) => (
              <li>{useCase}</li>
            ))}
          </ul>
        </div>
      )}

      {/* Pricing */}
      {data.pricing && (
        <div class="prose prose-slate max-w-none">
          <h2 class="text-3xl font-bold text-slate-900 mb-6">Pricing Plans</h2>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            {Object.entries(data.pricing).map(([plan, description]) => (
              <div class="bg-slate-50 p-6 rounded-lg border border-slate-200">
                <h3 class="font-bold text-lg text-slate-900 mb-2 capitalize">{plan}</h3>
                <p class="text-slate-700">{description}</p>
              </div>
            ))}
          </div>
        </div>
      )}

      {/* Pros & Cons */}
      {(data.pros || data.cons) && (
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
          {data.pros && (
            <div class="prose prose-slate max-w-none">
              <h3 class="text-2xl font-bold text-green-600 mb-4">✅ Pros</h3>
              <ul class="space-y-2 list-disc list-inside text-slate-700">
                {data.pros.map((pro) => (
                  <li>{pro}</li>
                ))}
              </ul>
            </div>
          )}
          {data.cons && (
            <div class="prose prose-slate max-w-none">
              <h3 class="text-2xl font-bold text-red-600 mb-4">❌ Cons</h3>
              <ul class="space-y-2 list-disc list-inside text-slate-700">
                {data.cons.map((con) => (
                  <li>{con}</li>
                ))}
              </ul>
            </div>
          )}
        </div>
      )}

      {/* Alternatives */}
      {data.alternatives && data.alternatives.length > 0 && (
        <div class="prose prose-slate max-w-none">
          <h2 class="text-3xl font-bold text-slate-900 mb-6">Alternatives to {data.name || data.title}</h2>
          <div class="space-y-3">
            {data.alternatives.map((alt) => {
              const altSlug = toolSlugMap[alt.name.toLowerCase()];
              return (
                <div class="flex items-start gap-3 text-lg text-slate-700">
                  <span class="text-blue-600 font-bold">→</span>
                  <div>
                    {altSlug ? (
                      <a href={`/tool/${altSlug}/`} class="font-bold text-blue-600 hover:underline">{alt.name}</a>
                    ) : (
                      <strong>{alt.name}</strong>
                    )} — {alt.reason}
                  </div>
                </div>
              );
            })}
          </div>
        </div>
      )}

      {/* FAQ */}
      {data.faqItems && data.faqItems.length > 0 && (
        <div class="prose prose-slate max-w-none">
          <h2 class="text-3xl font-bold text-slate-900 mb-6">Frequently Asked Questions</h2>
          <div class="space-y-6">
            {data.faqItems.map((item, idx) => (
              <div key={idx} class="bg-slate-50 p-6 rounded-lg border border-slate-200">
                <h3 class="font-bold text-lg text-slate-900 mb-2">{item.question}</h3>
                <p class="text-slate-700">{item.answer}</p>
              </div>
            ))}
          </div>
        </div>
      )}

      {/* Related Tools */}
      {relatedTools.length > 0 && (
        <div class="border-t border-slate-200 pt-12">
          <h2 class="text-3xl font-bold text-slate-900 mb-6">Similar {catLabel} Tools</h2>
          <div class="grid grid-cols-1 sm:grid-cols-3 gap-6">
            {relatedTools.map((t) => {
              const tSlug = t.id.replace(/\.md$/, '');
              return (
                <div class="bg-white rounded-xl shadow-md border border-slate-200 hover:border-blue-300 hover:shadow-lg transition-all flex flex-col overflow-hidden">
                  <div class="p-6 flex flex-col flex-grow">
                    <div class="flex items-center gap-3 mb-3">
                      <img
                        src={`https://www.google.com/s2/favicons?sz=32&domain=${t.data.url}`}
                        alt={`${t.data.title} logo`}
                        class="w-8 h-8 rounded flex-shrink-0"
                      />
                      <h3 class="font-bold text-slate-900 group-hover:text-blue-600">{t.data.title}</h3>
                    </div>
                    {t.data.pricingModel && (
                      <span class={`self-start mb-3 px-2.5 py-1 text-xs font-medium rounded-lg ${
                        t.data.pricingModel === 'Free' ? 'bg-green-100 text-green-700' :
                        t.data.pricingModel === 'Freemium' ? 'bg-blue-100 text-blue-700' :
                        'bg-purple-100 text-purple-700'
                      }`}>{t.data.pricingModel}</span>
                    )}
                    <p class="text-sm text-slate-600 flex-grow line-clamp-2 mb-4">{t.data.description}</p>
                  </div>
                  <div class="px-6 py-4 border-t border-slate-100 bg-slate-50 flex gap-2">
                    <a href={`/tool/${tSlug}/`} class="flex-1 text-center text-sm font-semibold px-3 py-2 bg-blue-50 text-blue-600 rounded-lg hover:bg-blue-100 border border-blue-200 transition-colors">
                      Learn More
                    </a>
                    <a href={`/go/${tSlug}/`} target="_blank" rel="noopener noreferrer" class="flex-1 text-center text-sm font-semibold px-3 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors">
                      Open →
                    </a>
                  </div>
                </div>
              );
            })}
          </div>
        </div>
      )}
    </div>
  </section>
</Layout>
